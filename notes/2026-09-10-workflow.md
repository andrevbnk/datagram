# Договорённости по процессу работы над Datagram

Зафиксировано: 2026-09-10.

## Организация

- **Рабочая папка топика** = корень репозитория: `C:\Users\admin\Desktop\TG\datagram-main`.
- Заметки и договорённости — в `notes/`.
- `AGENTS.md` в корне — принципы работы агента (авто-подгружается).

## Git-процесс

- Ветка по умолчанию для наследования: `develop`.
- PR по умолчанию создаётся в `develop`.
- `main` — стабильная ветка.
- Ветка `develop` создана из `main` (2026-09-10), т.к. в репозитории изначально была только `main`.

## GitHub

- Репозиторий: https://github.com/andrevbnk/datagram
- Аккаунт: `andrevbnk` (Kuzar).
- Авторизация: `GITHUB_TOKEN` в `~/.hermes/.env` (PAT), git push через credential helper.
- PR создаются через GitHub REST API.

## Открытые вопросы / на будущее

- (нет)
